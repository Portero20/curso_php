<?php 

    //Operador igual

    // $raza = "Husky";

    // var_dump($raza == 'Husky');

    //Operador identico

    // $edad = "18";

    // var_dump($edad != 18);

    //Operador diferente

    // $edad = 18;

    // var_dump($edad !== 18)

    //Operador no identico

    // $edad = 18;

    // var_dump($edad !== 18)

    //Operador >

    // $edad = 15;
    
    // var_dump($edad > 18);

    //Operador >=

    // $edad = 18;
    
    // var_dump($edad >= 18);

    //Operador <

    // $edad = 10;

    // var_dump($edad < 6);

    //Operador <=

    // $edad = 6;

    // var_dump($edad <= 6);
    
    //Operador de nave espacial

    $edad = 4;

    var_dump($edad <=> 5); //igual a 0

    var_dump($edad <=> 4); //igual a 1 porque es el 6 es mayor a 6

    var_dump($edad <=> 6); //igual a -1 si es que en la variable edad ponemos porque porque es menor

?>